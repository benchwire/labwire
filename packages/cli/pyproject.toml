[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "labwire-cli"
version = "0.2.0.dev0"
description = "Labwire command-line tools: verify signed run bundles"
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
authors = [{ name = "Silous Ramelli" }]
dependencies = ["labwire-core", "typer>=0.12"]

[project.scripts]
labwire = "labwire.cli.main:main"

[tool.uv.sources]
labwire-core = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/labwire"]
